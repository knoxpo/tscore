[package]
name = "tsp-macos"
description = "macOS platform layer"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2"
